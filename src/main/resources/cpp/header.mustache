#pragma once

#include <string>
#include <vector>

class {{{modelName}}} {
public:
    {{{modelName}}}() = default;

    {{#members}}
    {{#isPrimitive}}
    {{{dataType}}} {{{getterName}}}() const;
    void {{{setterName}}}({{{dataType}}} value);
    {{/isPrimitive}}
    {{^isPrimitive}}
    {{{dataType}}}& {{{getterName}}}() const;
    void {{{setterName}}}(const {{{dataType}}}& value);
    {{/isPrimitive}}

    {{/members}}
private:
    {{#members}}
        /* {{{description}}} */
        {{{dataType}}} {{{name}}};
    {{/members}}
};